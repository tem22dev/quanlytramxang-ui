export interface Routes {
    login: string;
    dashboard: string;
    accounts: string;
}

export const routes: Routes = {
    login: '/dang-nhap',
    dashboard: '/',
    accounts: '/tai-khoan',
};
